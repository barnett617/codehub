Function.prototype.myBind = function(context = window, ...args) {
  let originFunc = this;
  let bindArgs = [...args];
  return function() {
    let execArgs = Array.from(arguments);
    let result = originFunc.apply(context, bindArgs.concat(execArgs));
    return result;
  }
}